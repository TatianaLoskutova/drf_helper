name: Django-app workflow

on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands to deploy
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USERNAME }}
        password: ${{ secrets.SSH_PASSWORD }}
        script: |
          # Выполняет деплой
          usr/bin/bash -c "source venv/bin/activate"
          usr/bin/bash -c "git pull"
          usr/bin/bash -c "pip install -r requirements.txt"
          usr/bin/bash -c "python manage.py makemigrations"
          usr/bin/bash -c "python manage.py migrate"
          usr/bin/bash -c "sudo service ${{ secrets.GUNICORN }} restart"
          usr/bin/bash -c "sudo service nginx restart"
          # cd ${{ secrets.PATH }}
          # source venv/bin/activate
          # git pull
          # pip install -r requirements.txt
          # python manage.py makemigrations
          # python manage.py migrate
          # sudo service ${{ secrets.GUNICORN }} restart
          # sudo service nginx restart
